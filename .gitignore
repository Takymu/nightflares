# Исходные фото (оригиналы) — на сайт не нужны, весят много.
# На сайте используются уже пережатые версии в img/.
photos/

# Реальные ИНН/ФИО и прочие приватные данные — не заливаем, пока не откроемся
secrets.local.json
*.local.json

# Демо-видео: в репозиторий только используемое на сайте
demo/*.mp4
demo/*.mov
!demo/cut_looped.mp4

# Личные настройки Claude Code (разрешения и т.п.) — не в публичный репо
.claude/settings.local.json

# Системный мусор
Thumbs.db
.DS_Store
desktop.ini
